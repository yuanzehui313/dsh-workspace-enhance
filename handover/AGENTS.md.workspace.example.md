# 工作区指令（<工作区路径>）

> 用法：复制本文件为工作区根目录的 `AGENTS.md`，把占位符替换成实际内容；
> 新会话开局会自动注入（锚定类 preset 在晋升后延迟一步注入）。

## 本工作区是什么
<一句话说明本目录的用途。若不是代码库，写明源码位置、构建/部署命令与关键路径>

## 会话连续性协议（必须遵守）
1. 会话开始：先读 .dsh/HANDOFF.md，再看各相关仓库的 git 状态与最近提交，再动手。
2. 每个里程碑/任务结束：更新 .dsh/HANDOFF.md（当前状态、已完成、待办、风险、关键命令与路径）。
3. 长期记忆：当工具列表里有 mcp__openviking__* 时，开工先 recall/find 检索相关记忆；收工把结论与决策 remember 写入 OpenViking。
4. 大块工作用 subagent 委托（上下文独立，只回传结果）；跨轮次长期目标用 goal 工具；上下文快满时优先分叉/合并会话，而不是丢失线索。
5. 涉及生产数据库、git push、删除等操作前，先向用户确认。

## 关键分支与产物
- <当前主线分支 / 部署产物路径 / 禁止执行的清单等，按项目填写>
